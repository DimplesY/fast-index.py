# overwrite default value to this program
app: "main:app"
port: 8000
allowed_hosts: ["*"]
cors_allow_origins: ["*"]
cors_allow_methods: ["*"]
cors_allow_credentials: true

# 数据库配置
datasource_url: "mysql://root:yanjie@localhost:3306/jpa?min_size=5&max_size=20"

# 开发环境
dev:
  "host": "127.0.0.1"
  "debug": true
  "log_level": "debug"

# 生产环境
pro:
  "host": "127.0.0.1"
  "port": 80
  "log_level": "warning"

# 测试环境
test:
  log_level: "debug"